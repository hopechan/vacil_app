name: vacil_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  font_awesome_flutter: ^8.2.0
  #dependencias para auntenticacion 
  firebase_auth:
  google_sign_in:
  cloud_firestore: 
  http: ^0.12.0
  #dependencias para mapas y ruteo
  location: ^1.4.1
  flutter_map:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/