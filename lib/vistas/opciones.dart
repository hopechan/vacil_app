import 'package:flutter/material.dart';

class Opciones extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: Text("Opciones")),
      body: new Center(
        child: new Text("Página de opnciones"),
        
      ),
      
    );
  }
}